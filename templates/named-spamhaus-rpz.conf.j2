{{ ansible_managed | comment('c') }}
// The locally defined rpz zone

zone "rpz.spamhaus.org" in
{
	type slave;
	masters {
		// redacted: spamhaus rpz DNS (paid service)
	};
	file "rpz.spamhaus.org.zone";
	allow-query { clients; };
};
